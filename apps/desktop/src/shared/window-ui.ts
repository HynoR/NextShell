/**
 * Windows title bar overlay height used by frameless header-safe layouts.
 * Keep this as the single source of truth for both main and renderer/preload.
 */
export const WINDOWS_TITLEBAR_SAFE_TOP = 44;
